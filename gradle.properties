# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# Fabric Properties are handled per version

# Mod Properties
mod_version=1.0.0
maven_group=net.prouncedev.striply
archives_base_name=striply

# Dependencies
fabric_version=0.102.0+1.21